# Örnek ortam değişkenleri
PORT=5000
NODE_ENV=development

# MongoDB bağlantı dizesi
MONGO_URI=mongodb://127.0.0.1:27017/hayat-agi

# JWT için gizli anahtar
JWT_SECRET=degistir-bunu

# (Opsiyonel) MQTT yapılandırması
MQTT_URL=mqtt://localhost:1883
MQTT_TOPIC=gateways/+/status
